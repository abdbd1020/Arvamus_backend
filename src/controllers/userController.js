const dbConnection = require("../database");
const crypto = require("crypto");

async function testApi() {
  try {
    // Output: Hello, world!
  } catch (e) {
    console.log(e);
  }
}

module.exports = {
  testApi,
};
